---
title: List all customers
description: Retrieve all customers
api: GET /api/external/customer
---

Retrieve a list of all customers associated with your business account.

## Headers

<ParamField header="x-blaaiz-api-key" type="string" required>
  Your API key for authentication
</ParamField>

## Query Parameters

<ParamField query="status" type="string">
  Filter customers by status. Options: `pending`, `verified`, `rejected`
</ParamField>

<ParamField query="type" type="string">
  Filter customers by type. Options: `individual`, `business`
</ParamField>

<ParamField query="page" type="number">
  Page number for pagination (default: 1)
</ParamField>

<ParamField query="limit" type="number">
  Number of results per page (default: 20, max: 100)
</ParamField>

## Response

<ResponseField name="status" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="message" type="string">
  Response message
</ResponseField>

<ResponseField name="data" type="object">
  Paginated customer data
  
  <Expandable title="Pagination object">
    <ResponseField name="customers" type="array">
      Array of customer objects
    </ResponseField>
    
    <ResponseField name="pagination" type="object">
      Pagination metadata
      
      <Expandable title="Pagination metadata">
        <ResponseField name="current_page" type="number">
          Current page number
        </ResponseField>
        
        <ResponseField name="total_pages" type="number">
          Total number of pages
        </ResponseField>
        
        <ResponseField name="total_count" type="number">
          Total number of customers
        </ResponseField>
        
        <ResponseField name="per_page" type="number">
          Number of results per page
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl --request GET \
  --url 'https://api-dev.blaaiz.com/api/external/customer?status=verified&limit=10' \
  --header 'x-blaaiz-api-key: YOUR_API_KEY'
```

```javascript Node.js
const response = await fetch('https://api-dev.blaaiz.com/api/external/customer?status=verified&limit=10', {
  method: 'GET',
  headers: {
    'x-blaaiz-api-key': 'YOUR_API_KEY'
  }
});

const data = await response.json();
```

```python Python
import requests

url = "https://api-dev.blaaiz.com/api/external/customer"
headers = {
    "x-blaaiz-api-key": "YOUR_API_KEY"
}
params = {
    "status": "verified",
    "limit": 10
}

response = requests.get(url, headers=headers, params=params)
data = response.json()
```
</RequestExample>

<ResponseExample>
```json 200 Success
{
  "status": true,
  "message": "Customers retrieved successfully",
  "data": {
    "customers": [
      {
        "id": "customer_123",
        "type": "individual",
        "first_name": "John",
        "last_name": "Doe",
        "email": "john@example.com",
        "phone_number": "+2348012345678",
        "status": "verified",
        "created_at": "2024-01-15T10:30:00Z"
      },
      {
        "id": "customer_456",
        "type": "business",
        "business_name": "Acme Corp",
        "email": "contact@acme.com",
        "phone_number": "+2348012345679",
        "status": "verified",
        "created_at": "2024-01-14T09:20:00Z"
      }
    ],
    "pagination": {
      "current_page": 1,
      "total_pages": 5,
      "total_count": 50,
      "per_page": 10
    }
  }
}
```

```json 401 Unauthorized
{
  "status": false,
  "message": "Invalid or missing API key"
}
```
</ResponseExample>
